import React from "react";
import { st, classes } from "./PageForm.st.css";

export type PageFormProps = {};
const PageForm = ({}: PageFormProps) => {
  return <div className={st(classes.root)}>FormModal</div>;
};

export default PageForm;
